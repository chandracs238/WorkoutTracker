name: Verify GitHub Actions Setup

on: [push]

jobs:
  verify-setup:
    runs-on: ubuntu-latest

    steps:
      - name: ✅ Workflow is Running
        run: echo "🎉 GitHub Actions is set up and working perfectly!"
